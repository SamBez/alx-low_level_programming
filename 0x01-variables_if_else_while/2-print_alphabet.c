#include <main.h>
#include <stdlib.h>
/**
 * main - mainf
 * Return: 0 forssomething
 */
int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
